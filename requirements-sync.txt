# Only needed for the one-off setup and fixture scripts. The app itself reads
# the sheet through the Sheets API in TypeScript and needs no Python.
openpyxl>=3.1.5   # scripts/dump_fixture.py
requests>=2.32.3  # scripts/setup_vercel_env.py
